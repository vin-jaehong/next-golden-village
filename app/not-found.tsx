import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Not found Page!",
};

const NotFound = () => {
  return (
    <div>
      <h1>Not Found!</h1>
    </div>
  );
};

export default NotFound;
